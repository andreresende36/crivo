{
  "name": "crivo",
  "version": "1.0.0",
  "description": "Scraper de ofertas ML · score engine · bots Telegram/WhatsApp · geração de imagens IA · painel admin Next.js",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.19.0"
  },
  "workspaces": [
    ".claude/mcp-servers/*"
  ],
  "scripts": {
    "codegen": "bash scripts/codegen.sh",
    "build": "pnpm --filter '@crivo/types' build && pnpm --filter '@crivo/admin' build",
    "dev": "pnpm --filter '@crivo/admin' dev",
    "test:admin": "pnpm --filter '@crivo/admin' lint",
    "claude:index": "tsx .claude/scripts/index-project.ts src",
    "mcp:build": "npm run build --workspaces --if-present",
    "mcp:install": "npm install --workspaces"
  }
}
